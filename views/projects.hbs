<h1>PROJECTS</h1>

{{#if projects.length}}
  <ul>
    {{#each projects}}
      <li>
        <a href="/projects/{{id}}">
          {{title}}</a>
      </li>{{/each}}
  </ul>

{{else}}
  <p>Couldn't find any projects</p>
{{/if}}